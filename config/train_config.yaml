defaults:
  - _self_
  - env: maze2d-large
  - trainer: ddpm
format_strs:
  - stdout
  - torch-tensorboard
hydra:
  run:
    dir: ./logs/${hydra:runtime.choices.trainer}/${env.name}/${trainer.diffusion.name}/${trainer.model.name}/${now:%Y%m%d-%H%M%S}